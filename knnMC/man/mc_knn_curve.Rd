% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function.R
\name{mc_knn_curve}
\alias{mc_knn_curve}
\title{Compute an "optimal k" curve by scanning k values}
\usage{
mc_knn_curve(
  k_values,
  n = 200,
  B = 500,
  sigma = 0.2,
  d = 1,
  type = c("1d", "2d"),
  grid_size = 101,
  seed = NULL,
  target = c("y", "f")
)
}
\arguments{
\item{k_values}{Integer vector. Candidate k values.}

\item{n, B, sigma, d, type, grid_size, seed}{Passed to \code{mc_knn_decomp()}.}

\item{target}{Character. \code{"f"} to minimize \eqn{MSE_f}, \code{"y"} to minimize \eqn{MSE_Y}.}
}
\value{
A data frame with k and the average MSE over grid points.
}
\description{
Runs \code{mc_knn_decomp()} for each k in \code{k_values} and summarizes the integrated MSE over the grid.
}
\examples{
curve <- mc_knn_curve(k_values = c(1, 3, 5), n = 50, B = 50, sigma = 0.2, d = 1, type = "1d", grid_size = 51, seed = 1)
curve

}
