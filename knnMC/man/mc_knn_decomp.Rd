% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function.R
\name{mc_knn_decomp}
\alias{mc_knn_decomp}
\title{Monte Carlo estimate of bias–variance decomposition for k-NN}
\usage{
mc_knn_decomp(
  n = 200,
  k = 10,
  B = 500,
  sigma = 0.2,
  d = 1,
  type = c("1d", "2d"),
  grid_size = 101,
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Training sample size.}

\item{k}{Integer. Number of neighbors.}

\item{B}{Integer. Number of Monte Carlo repetitions.}

\item{sigma}{Numeric. Noise standard deviation.}

\item{d}{Integer. Dimension (1 or 2).}

\item{type}{Character. \code{"1d"} or \code{"2d"}.}

\item{grid_size}{Integer. Grid resolution (see \code{make_grid()}).}

\item{seed}{Optional integer. If provided, sets the random seed for reproducibility.}
}
\value{
A list with the grid, true function values, and Monte Carlo estimates of bias, variance,
and MSE components at each grid point, plus a summary curve (integrated MSE over the grid).
}
\description{
Runs B independent simulations from the DGP, fits k-NN, and evaluates predictions on a grid.
}
\details{
Definitions (at each grid point x):
\itemize{
\item \eqn{\mathrm{Bias}(x) = E[\hat f(x)] - f(x)}
\item \eqn{\mathrm{Var}(x) = \mathrm{Var}(\hat f(x))}
\item \eqn{\mathrm{MSE}_f(x) = \mathrm{Bias}(x)^2 + \mathrm{Var}(x)} (for estimating f)
\item \eqn{\mathrm{MSE}_Y(x) = \mathrm{MSE}_f(x) + \sigma^2} (for predicting Y; adds irreducible error)
}
}
\examples{
res <- mc_knn_decomp(n = 50, k = 5, B = 50, sigma = 0.2, d = 1, type = "1d", grid_size = 51, seed = 1)
names(res)
head(res$pointwise)

}
